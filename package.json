{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.15.0",
    "@redneckz/react-lib-config": "0.0.7"
  },
  "scripts": {
    "prepublish": "lerna run clean && lerna run eslint && lerna run jest",
    "test": "lerna run test"
  }
}
